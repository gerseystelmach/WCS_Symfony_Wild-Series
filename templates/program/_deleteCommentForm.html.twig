<form method="post" action="{{ path('program_comment_delete', {'comment_id': comment.id}) }}" onsubmit="return confirm('Are you sure you want to delete this comment?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ comment.id) }}">
    <button class="btn btn-danger mt-4">Delete</button>
</form>

