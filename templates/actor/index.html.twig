{% extends 'base.html.twig' %}

{% block title %}Wild Séries{% endblock %}

{% block body %}
<main class="container">
    <h1 class="text-center my-5">Acteurs :</h1>

    {% for actor in actors %}
    <div class="d-block p-3">
   {{ loop.index }} - <a  href=""> {{ actor.name }}</a>
        </div>
    {% else %}
        <p>No actor found.</p>
    {% endfor %}
</main>


{% endblock %}
